---
import BaseLayout from '../layouts/BaseLayout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
import PortfolioCTA from '../components/PortfolioCTA.astro';
---

<BaseLayout
  title="Video Portfolio | Frame Of Rangga"
  description="Portofolio videografi Rangga Danuarta — brand video, dokumentasi event, konten sosial media, dan video promosi dengan pendekatan cinematic storytelling."
  ogImage="https://for-portfolio-mu.vercel.app/assets/og/og-video.jpg"
  ogUrl="https://for-portfolio-mu.vercel.app/video"
  hasGlightbox={true}
>
  <Navbar slot="navbar" activePage="video" />

  <!-- PAGE HERO -->
  <section class="page-hero" transition:animate="fade">
    <div class="page-hero-bg-glow" aria-hidden="true"></div>
    <div class="page-hero-inner">
      <span class="section-label gradient-label">Portfolio</span>
      <h1 class="page-hero-title">Video Portfolio</h1>
      <p class="page-hero-sub">Koleksi video brand, dokumentasi event, dan konten kreatif dengan pendekatan cinematic storytelling.</p>
    </div>
  </section>

  <section class="video-section" transition:animate="fade">
    <!-- Update notice -->
    <div class="update-notice fade-up">
      <i class="fas fa-film"></i>
      <span>Video portfolio sedang dalam proses update. <a href="https://wa.me/62895321693131" target="_blank" rel="noopener noreferrer">Hubungi via WhatsApp</a> untuk melihat showreel lengkap.</span>
    </div>

    <!-- Filter Bar -->
    <div class="filter-bar fade-up" aria-label="Filter by category">
      <button class="filter-btn active" data-filter="all">Semua</button>
      <button class="filter-btn" data-filter="brand">Brand Video</button>
      <button class="filter-btn" data-filter="dokumentasi">Dokumentasi</button>
      <button class="filter-btn" data-filter="sosmed">Konten Sosial Media</button>
      <button class="filter-btn" data-filter="promosi">Promosi</button>
    </div>

    <!-- Video Grid -->
    <div class="video-grid fade-up">

      <!-- Card 1: Brand Video -->
      <a class="glightbox video-card"
         href="https://www.youtube.com/watch?v=dQw4w9WgXcQ"
         data-type="video"
         data-source="youtube"
         data-gallery="videos"
         data-category="brand">
        <div class="video-thumb-wrap">
          <img src="/assets/thumbnails/ss keyboard.png" alt="Brand Video" loading="lazy" />
          <div class="play-overlay">
            <div class="play-btn">
              <i class="fas fa-play play-icon"></i>
            </div>
          </div>
          <span class="video-duration-badge">3:45</span>
        </div>
        <div class="video-card-body">
          <span class="video-cat-label">Brand Video</span>
          <h3 class="video-card-title">Brand Video: Client Project</h3>
          <p class="video-card-desc">Video campaign dengan pendekatan cinematic storytelling untuk brand.</p>
        </div>
      </a>

      <!-- Card 2: Dokumentasi -->
      <a class="glightbox video-card"
         href="https://www.youtube.com/watch?v=dQw4w9WgXcQ"
         data-type="video"
         data-source="youtube"
         data-gallery="videos"
         data-category="dokumentasi">
        <div class="video-thumb-wrap">
          <img src="/assets/thumbnails/ss laptop.png" alt="Dokumentasi Event" loading="lazy" />
          <div class="play-overlay">
            <div class="play-btn">
              <i class="fas fa-play play-icon"></i>
            </div>
          </div>
          <span class="video-duration-badge">5:20</span>
        </div>
        <div class="video-card-body">
          <span class="video-cat-label">Dokumentasi</span>
          <h3 class="video-card-title">Dokumentasi Event</h3>
          <p class="video-card-desc">Coverage acara dan event dengan hasil yang dinamis dan emosional.</p>
        </div>
      </a>

      <!-- Card 3: Konten Sosial Media -->
      <a class="glightbox video-card"
         href="https://www.youtube.com/watch?v=dQw4w9WgXcQ"
         data-type="video"
         data-source="youtube"
         data-gallery="videos"
         data-category="sosmed">
        <div class="video-thumb-wrap">
          <img src="/assets/thumbnails/web1.png" alt="Konten Sosmed" loading="lazy" />
          <div class="play-overlay">
            <div class="play-btn">
              <i class="fas fa-play play-icon"></i>
            </div>
          </div>
          <span class="video-duration-badge">0:30</span>
        </div>
        <div class="video-card-body">
          <span class="video-cat-label">Konten Sosial Media</span>
          <h3 class="video-card-title">Konten Sosial Media</h3>
          <p class="video-card-desc">Short-form content yang engaging untuk Instagram, TikTok, dan YouTube Shorts.</p>
        </div>
      </a>

      <!-- Card 4: Promosi -->
      <a class="glightbox video-card"
         href="https://www.youtube.com/watch?v=dQw4w9WgXcQ"
         data-type="video"
         data-source="youtube"
         data-gallery="videos"
         data-category="promosi">
        <div class="video-thumb-wrap">
          <img src="/assets/thumbnails/web2.png" alt="Video Promosi" loading="lazy" />
          <div class="play-overlay">
            <div class="play-btn">
              <i class="fas fa-play play-icon"></i>
            </div>
          </div>
          <span class="video-duration-badge">1:00</span>
        </div>
        <div class="video-card-body">
          <span class="video-cat-label">Promosi</span>
          <h3 class="video-card-title">Video Promosi Produk</h3>
          <p class="video-card-desc">Video iklan yang langsung nge-hit target audiens dan boost conversion.</p>
        </div>
      </a>

    </div>
  </section>

  <PortfolioCTA />

  <Footer slot="footer" variant="minimal" />
</BaseLayout>

<script>
  /* ---------- GLIGHTBOX (YouTube embed) ---------- */
  document.addEventListener('DOMContentLoaded', function initGL() {
    if (typeof GLightbox === 'undefined') {
      setTimeout(initGL, 100);
      return;
    }
    GLightbox({
      selector: '.glightbox',
      touchNavigation: true,
      loop: false,
      autoplayVideos: true,
    });
  });

  /* ---------- VIDEO FILTER ---------- */
  document.addEventListener('DOMContentLoaded', function init() {
    const filterBar = document.querySelector('.filter-bar');
    if (!filterBar) return;

    const btns = filterBar.querySelectorAll('.filter-btn');
    const grid = document.querySelector('.video-grid');
    if (!grid) return;

    btns.forEach(function(btn) {
      btn.addEventListener('click', function() {
        const filter = btn.dataset.filter;

        // Update active button
        btns.forEach(function(b) { b.classList.remove('active'); });
        btn.classList.add('active');

        // Show/hide cards
        const cards = grid.querySelectorAll('.video-card');
        cards.forEach(function(card) {
          const cat = card.dataset.category || '';
          if (filter === 'all' || cat === filter) {
            card.style.display = '';
          } else {
            card.style.display = 'none';
          }
        });
      });
    });
  });
</script>
